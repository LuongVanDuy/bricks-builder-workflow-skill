interface:
  display_name: "Bricks Builder Workflow"
  short_description: "Build native Bricks sites from compact inputs"
  default_prompt: "Use $bricks-builder-workflow. Choose one route, create the requested native Bricks artifact, validate once, and stop."

policy:
  allow_implicit_invocation: true
